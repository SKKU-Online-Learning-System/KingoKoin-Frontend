const NoData = () => {
  return (
    <div className="flex flex-col items-center justify-center gap-2 w-full h-full">
      <p className="text-darkGray">보여줄 데이터가 존재하지 않습니다.</p>
    </div>
  );
};

export default NoData;
